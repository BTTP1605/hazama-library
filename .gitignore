node_modules/
dist/
*.local

# 有料章のシナリオ原文（平文）。公開リポジトリに含めないこと。
scenario-src/paid/

# 暗号化済み有料章バンドル。ローカルでは開発用コード、デプロイ時は本番コードで
# その都度生成されるため、リポジトリには含めない（デプロイ物にのみ同梱される）。
public/scenario/paid.enc

# 本番用パスフレーズのメモ（ローカル専用）
passphrase.local
